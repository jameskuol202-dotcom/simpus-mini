# SIMPUS-Mini Jobsheets

This repository is organized into two separate folders:

- `jobsheet-01/` for the plain HTML version
- `jobsheet-02/` for the CSS-styled version

Open the `index.html` file inside the folder you want to view.
